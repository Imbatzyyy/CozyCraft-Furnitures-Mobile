# CozyCraft Mobile 1.0.37

The loading sofa now has alternating cushion hops, a soft settling bounce, and a matching ground shadow. The launch version is larger while inline loaders retain their compact sizing. Economy mode keeps a single lightweight animation; Reduce Motion disables movement. All artwork is bundled SVG with CSS transforms and no additional network requests. The existing branded splash remains first in the launch flow.
